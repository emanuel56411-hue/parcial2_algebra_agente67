[project]
name = "techchip-matrix-studio"
version = "1.0.0"
description = "Analizador exacto y explicable de sistemas lineales"
requires-python = ">=3.12"
dependencies = []

[tool.vercel]
entrypoint = "api.solve:handler"
