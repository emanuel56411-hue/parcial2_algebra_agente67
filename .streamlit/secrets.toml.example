# Copia este archivo a secrets.toml. Nunca subas tu clave a Git.
OPENAI_API_KEY = ""
OPENAI_MODEL = "gpt-4.1-mini"
# Cuota compartida entre sesiones del mismo servidor, por día UTC.
OPENAI_DAILY_REQUEST_LIMIT = 50
